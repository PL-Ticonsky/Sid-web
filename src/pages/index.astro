---
import BaseLayout from "../layouts/BaseLayout.astro";
import site from "../data/site.json";

const BASE = import.meta.env.BASE_URL;

const gallery = [
  `${BASE}images/activities/demo.webp`,
  `${BASE}images/activities/demo.webp`,
  `${BASE}images/activities/demo.webp`,
  `${BASE}images/activities/demo.webp`,
];

// Ahora es "Contáctanos"
const resources = [
  { label: "Correo institucional", url: "mailto:sid@correo.edu.co" },
  { label: "Formulario de contacto", url: "https://forms.gle/" },
  { label: "Repositorio (GitHub)", url: "https://github.com/" },
  { label: "Redes sociales", url: "https://instagram.com/" },
];
---

<BaseLayout title="SID · Inicio">
  <!-- HERO -->
  <section class="py-16">
    <div class="grid md:grid-cols-2 items-center gap-10">
      <!-- Texto -->
      <div>
        <h1 class="font-poppins text-4xl md:text-5xl text-sid-navy">
          Sociedad de Innovación y Desarrollo
        </h1>

        <p class="mt-4 text-slate-700 leading-relaxed max-w-xl">
          Iniciativa estudiantil de la Universidad Distrital Francisco José de Caldas orientada a la formación aplicada,
          la experimentación responsable y el desarrollo de proyectos con impacto académico y social.
        </p>

        <div class="mt-6 flex gap-3">
          <a href={site.joinUrl} class="bg-sid-pink px-5 py-2.5 rounded-xl text-white font-semibold">
            Únete
          </a>
        </div>
      </div>

      <!-- Pacha -->
      <div class="flex justify-center md:justify-end">
        <img
          src={`${BASE}images/brand/pachas.webp`}
          alt="Pacha, mascota oficial de SID"
          class="w-64 md:w-80 drop-shadow-xl"
        />
      </div>
    </div>
  </section>

  <section class="py-10">
    <div class="rounded-3xl border border-gray-200 bg-white p-8 md:p-12">
      <h1 class="font-poppins text-4xl md:text-5xl leading-tight text-sid-navy">
        Sociedad de Innovación y Desarrollo
      </h1>

      <p class="mt-4 max-w-2xl text-slate-700 leading-relaxed">
        Somos un proyecto académico estudiantil: organizamos actividades prácticas, compartimos formación estructurada y
        documentamos lo aprendido. El objetivo es reducir la brecha entre teoría y práctica mediante trabajo colaborativo
        y procesos claros.
      </p>

      <div class="mt-7 flex flex-wrap gap-3">
        <a
          href={site.joinUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center justify-center rounded-xl bg-sid-pink px-5 py-2.5 text-sm font-semibold text-white hover:opacity-90 transition"
        >
          Únete
        </a>

        <a
          href={`${BASE}activities/`}
          class="inline-flex items-center justify-center rounded-xl border border-gray-200 px-5 py-2.5 text-sm font-semibold text-sid-navy hover:bg-gray-50 transition"
        >
          Ver actividades
        </a>

        <a
          href={`${BASE}courses/`}
          class="inline-flex items-center justify-center rounded-xl border border-gray-200 px-5 py-2.5 text-sm font-semibold text-sid-navy hover:bg-gray-50 transition"
        >
          Ver cursos
        </a>
      </div>
    </div>
  </section>

  <!-- QUÉ HACEMOS -->
  <section class="py-10 space-y-6">
    <h2 class="font-poppins text-2xl text-sid-navy">Misión y visión</h2>

    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
      {[
        {
          title: "Misión",
          desc: "Fortalecer la formación de estudiantes a través de aprendizaje aplicado, trabajo colaborativo y producción de entregables documentados (proyectos, guías y recursos).",
          href: site.joinUrl,
          external: true,
        },
        {
          title: "Visión",
          desc: "Consolidar un referente estudiantil en innovación y desarrollo dentro de la Universidad Distrital, con cultura de calidad, documentación y mejora continua.",
          href: site.joinUrl,
          external: true,
        },
        {
          title: "Líneas de trabajo",
          desc: "Actividades prácticas (hackatones, maratones y dinámicas), formación por cursos y acompañamiento entre pares para construir bases sólidas.",
          href: `${BASE}activities/`,
        },
        {
          title: "Principios",
          desc: "Rigor académico, transparencia, colaboración y enfoque en impacto. Priorizamos procesos simples, resultados verificables y comunicación clara.",
          href: "#recursos",
        },
      ].map((c) => (
        <a
          href={c.href}
          target={c.external ? "_blank" : undefined}
          rel={c.external ? "noopener noreferrer" : undefined}
          class="block rounded-2xl border border-gray-200 bg-white p-6 hover:shadow-md transition"
        >
          <div class="font-poppins font-semibold text-lg text-sid-navy">{c.title}</div>
          <p class="mt-2 text-sm leading-relaxed text-slate-700">{c.desc}</p>
        </a>
      ))}
    </div>
  </section>

  <!-- GALERÍA -->
  <section class="py-10 space-y-6">
    <h2 class="font-poppins text-2xl text-sid-navy">Galería</h2>

    <div class="flex gap-4 overflow-x-auto snap-x snap-mandatory pb-2">
      {gallery.map((src) => (
        <div class="snap-start min-w-[280px] sm:min-w-[360px] overflow-hidden rounded-2xl border bg-gray-100">
          <img src={src} alt="Galería de actividades SID" class="h-56 w-full object-cover" loading="lazy" />
        </div>
      ))}
    </div>
  </section>

  <!-- CONTÁCTANOS -->
  <section id="recursos" class="py-10 space-y-6">
    <h2 class="font-poppins text-2xl text-sid-navy">Contáctanos</h2>

    <p class="max-w-2xl text-slate-700 leading-relaxed">
      Para alianzas, propuestas de actividades, participación como ponente o vinculación al equipo, usa cualquiera de
      estos canales. Respondemos por orden de llegada.
    </p>

    <div class="space-y-3">
      {resources.map((r) => (
        <a
          href={r.url}
          target="_blank"
          rel="noopener noreferrer"
          class="block rounded-2xl border border-gray-200 bg-white p-5 hover:shadow-md transition"
        >
          <div class="font-semibold text-sid-blue">{r.label}</div>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>
